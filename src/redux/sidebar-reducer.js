let initialState = {
  friends: [
    { id: 1, name: 'Sveta' },
    { id: 2, name: 'Andrey' },
    { id: 3, name: 'Viktor' },
  ]
};

const sidebarReducer = (state = initialState, action) => {
  
  return state;
}

export default sidebarReducer;
